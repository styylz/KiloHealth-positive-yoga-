import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  typography: {
    fontFamily: [
      'SF Pro Display',
      'sans-serif',
    ].join(','),
  },
});

export default theme;
